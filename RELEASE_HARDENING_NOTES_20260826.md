# Installed TestFlight regressions promoted to release gates

The release-hardening branch is based on the installed TestFlight evidence collected on 2026-08-26.

## HCV-6052EF81478945A4

The signed final fusion is expected to remain `NON_CONCLUSIVE`, risk `MEDIUM`, score `69` when strong `SCREEN_MONITOR` ML conflicts with REALITY geometry. A lower-level live probe may still report geometric REALITY, but the public verification card must not render `Realtà rilevata` unless the final signed display fusion is `NO_DISPLAY_EVIDENCE`.

## HCV-3F31A5EA164F4AE7

A mixed 3D physical scene containing a monitor produced live temporal/display cues, passive score 75 and `SCREEN_MONITOR` ML evidence while geometry resolved REALITY. This profile must not collapse to `NO_DISPLAY_EVIDENCE`; the release regression expects `NON_CONCLUSIVE`, risk `MEDIUM`, score `69`.

These tests intentionally preserve REALITY geometry as one evidence axis rather than treating REALITY and DISPLAY as mutually exclusive scene classes.
