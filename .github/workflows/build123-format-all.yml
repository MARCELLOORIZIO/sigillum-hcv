name: BUILD123 format all

on:
  pull_request:
    branches:
      - fix/build122-corpus-wide-display-recovery-20260919

permissions:
  contents: write

jobs:
  dart-format-all:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout BUILD123 head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.1'
          cache: true
      - run: flutter pub get
      - name: Format BUILD123 files
        run: |
          dart format             lib/hcv_context_free_display_policy.dart             lib/camera_page.dart             test/build123_context_free_display_policy_test.dart
          if git diff --quiet --             lib/hcv_context_free_display_policy.dart             lib/camera_page.dart             test/build123_context_free_display_policy_test.dart; then
            echo "BUILD123_FORMAT_ALREADY_CLEAN=PASS"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add             lib/hcv_context_free_display_policy.dart             lib/camera_page.dart             test/build123_context_free_display_policy_test.dart
          git commit -m "BUILD123 apply Dart formatter"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
