name: BUILD125 format all

on:
  pull_request:
    branches:
      - fix/build124-multi-evidence-display-fusion-20260920

permissions:
  contents: write

jobs:
  dart-format-all:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout BUILD125 head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.1'
          cache: true
      - run: flutter pub get
      - name: Format BUILD125 files
        run: |
          dart format             lib/camera_page.dart             lib/hcv_temporal_frequency_probe.dart             test/build125_hfr_zoom_parity_test.dart
          if git diff --quiet --             lib/camera_page.dart             lib/hcv_temporal_frequency_probe.dart             test/build125_hfr_zoom_parity_test.dart; then
            echo "BUILD125_FORMAT_ALREADY_CLEAN=PASS"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add             lib/camera_page.dart             lib/hcv_temporal_frequency_probe.dart             test/build125_hfr_zoom_parity_test.dart
          git commit -m "BUILD125 apply Dart formatter"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
